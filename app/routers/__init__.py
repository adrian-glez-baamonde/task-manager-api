# Sirve para indicar a Python que estas carpetas son paquetes (packages), 
# permitiendo hacer imports tipo from app.schemas import TaskCreate. 
# Es un archivo que puede estar completamente vacío, solo con su presencia ya cumple su función.